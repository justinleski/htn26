# The frontend uses same-origin /api routes and Shopify session cookies.
# Backend credentials, including Backboard keys, belong in apps/backend/.env.
# For a separate Vite dev server, set BACKEND_URL in the shell before npm run dev.
# The default proxy target is http://localhost:3000.
