# Documentation only: services share this root, so configure builds separately.
# api: Dockerfile.api; start `npm run docker-start`; healthcheck /health
# web: Dockerfile.web; start `node scripts/web-server.mjs`; healthcheck /health
# web runtime: PUBLIC_APP_URL=https://adgile.tech
#              API_UPSTREAM=https://api-production-aa9b.up.railway.app
